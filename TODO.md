# TODO

## TODO - NOT DONE

- Auto stop servers that have no players and start on demand
- Fix handling of containers failing (restart, but if they fail in a loop don't restart them)
- Allow subdomains pointing to specific servers bypassing the web auth flow (only for public servers)
- Restrict file names in buckets (getting the presigned url)
- Distributed runners (job assignment)

## TODO - DID

- Fix delete account handling (delete servers from rustfs)
- Add delete server button
- Implement server players list (query from proxy server)
